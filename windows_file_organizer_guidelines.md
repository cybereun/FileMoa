# Windows 파일 정리 기준 지침

## 1. 문서 목적

이 문서는 Windows 환경에서 파일을 자동 또는 반자동으로 정리하는 앱을 설계·개발하기 위한 기준 지침이다.

앱의 핵심 철학은 다음과 같다.

> **파일을 지우는 클리너가 아니라, 흩어진 파일을 안전하게 제자리에 정리해 주는 앱**

정리 흐름은 아래 4단계를 기본으로 한다.

1. 무엇을 정리할지 선택
2. 어떤 기준으로 분류할지 결정
3. 어떻게 이동·이름 변경할지 결정
4. 잘못됐을 때 어떻게 복구할지 보장

---

# 2. 정리 대상 폴더

사용자가 정리할 위치를 직접 선택할 수 있어야 한다.

## 기본 지원 위치

- 바탕화면
- 다운로드
- 문서
- 사진
- 동영상
- 음악
- 사용자 지정 폴더
- 외장하드
- USB
- 여러 폴더 동시 선택

## 추가 옵션

- 하위 폴더까지 포함
- 현재 폴더의 파일만 정리
- 숨김 파일 포함/제외
- 시스템 파일 제외
- OneDrive 폴더 포함/제외
- 네트워크 드라이브 포함/제외
- 외부 저장장치 포함/제외

## 기본 보호 대상

다음 위치는 기본적으로 정리 대상에서 제외한다.

- `C:\Windows`
- `C:\Program Files`
- `C:\Program Files (x86)`
- `C:\ProgramData`
- 사용자 AppData 주요 시스템 영역
- `.git`
- `node_modules`
- 시스템 복구 관련 폴더
- 애플리케이션 내부 데이터 폴더

---

# 3. 기본 파일 분류 체계

## 3.1 문서

추천 구조:

```text
문서
├─ PDF
├─ Word
├─ Excel
├─ PowerPoint
├─ 한글
├─ 텍스트
├─ 전자책
└─ 기타 문서
```

지원 확장자 예:

```text
pdf
doc
docx
xls
xlsx
csv
ppt
pptx
hwp
hwpx
txt
md
rtf
epub
```

한국 사용자 대상에서는 `HWP`, `HWPX` 지원을 필수로 권장한다.

---

## 3.2 이미지

추천 구조:

```text
이미지
├─ 사진
├─ 스크린샷
├─ PNG
├─ JPG
├─ GIF
├─ SVG
├─ WEBP
├─ RAW
└─ 기타
```

지원 확장자 예:

```text
jpg
jpeg
png
gif
webp
bmp
tiff
svg
heic
avif
raw
cr2
nef
arw
```

### 스크린샷 자동 분류

다음과 같은 파일명 패턴을 감지할 수 있다.

```text
Screenshot
스크린샷
SnippingTool
캡처
Capture
```

자동 분류 예:

```text
이미지/스크린샷
```

---

## 3.3 동영상

추천 구조:

```text
동영상
├─ MP4
├─ MOV
├─ AVI
├─ MKV
├─ 녹화영상
└─ 기타
```

지원 확장자 예:

```text
mp4
mov
avi
mkv
wmv
webm
m4v
mts
```

OBS, Xbox Game Bar, 화면 녹화 파일 등을 별도 카테고리로 분리할 수 있다.

---

## 3.4 음악 / 오디오

추천 구조:

```text
오디오
├─ 음악
├─ 녹음
├─ Podcast
└─ 기타
```

지원 확장자 예:

```text
mp3
wav
flac
aac
m4a
ogg
wma
```

파일명에 다음 단어가 포함될 경우 녹음 파일로 분류할 수 있다.

```text
Recording
녹음
Voice
Memo
```

---

## 3.5 압축파일

추천 구조:

```text
압축파일
├─ ZIP
├─ RAR
├─ 7Z
└─ 기타
```

지원 확장자 예:

```text
zip
rar
7z
tar
gz
bz2
xz
```

### 선택 기능

- 압축 자동 해제
- 압축 해제 후 원본 유지
- 압축 해제 후 원본 삭제

기본값은 반드시 **원본 유지**로 설정한다.

---

## 3.6 설치파일

추천 구조:

```text
설치파일
├─ EXE
├─ MSI
├─ MSIX
├─ App Installer
└─ 드라이버
```

지원 확장자 예:

```text
exe
msi
msix
msixbundle
appx
appxbundle
```

예:

```text
ChromeSetup.exe
VSCodeUserSetup.exe
DiscordSetup.exe
```

추가 조건 예:

- 30일 이상 된 설치파일 찾기
- 설치파일만 따로 모으기
- 최근 설치파일 유지
- 오래된 설치파일 정리 후보 표시

---

## 3.7 개발파일

추천 구조:

```text
개발
├─ Source Code
├─ Web
├─ Python
├─ Data
├─ Config
├─ Database
└─ Git
```

지원 확장자 예:

```text
js
jsx
ts
tsx
html
css
scss
py
java
kt
cpp
c
cs
go
rs
php
sql
json
yaml
yml
toml
xml
env
ipynb
```

### 개발 프로젝트 보호

다음 파일 또는 폴더를 발견하면 해당 디렉터리를 개발 프로젝트로 간주한다.

```text
package.json
.git
requirements.txt
pyproject.toml
Cargo.toml
pom.xml
build.gradle
composer.json
go.mod
CMakeLists.txt
```

개발 프로젝트 내부 파일은 확장자 기준으로 자동 이동하지 않는다.

기본 원칙:

> **프로젝트 구조를 깨뜨릴 가능성이 있으면 정리하지 않는다.**

---

## 3.8 기타 권장 카테고리

### 전자책

```text
epub
mobi
azw
azw3
```

### 폰트

```text
ttf
otf
woff
woff2
```

### 디스크 이미지

```text
iso
img
vhd
vhdx
```

### 데이터

```text
csv
json
xml
sqlite
db
parquet
```

### 기타

어떤 분류에도 속하지 않는 파일은 반드시 `기타`로 보낸다.

---

# 4. 분류 기준

사용자가 원하는 정리 기준을 선택할 수 있어야 한다.

## 4.1 파일 형식별

예:

```text
문서
이미지
동영상
음악
압축파일
설치파일
개발파일
기타
```

가장 기본적인 정리 방식이다.

---

## 4.2 날짜별

예:

```text
2026
├─ 01월
├─ 02월
├─ 03월
└─ 09월
```

또는:

```text
2026-09
2026-08
2026-07
```

---

## 4.3 연도별

예:

```text
2023
2024
2025
2026
```

사진, 문서 보관에 유용하다.

---

## 4.4 날짜 기준 종류

사용자가 다음 기준 중 하나를 선택할 수 있어야 한다.

- 생성 날짜
- 수정 날짜
- 마지막 사용 날짜
- 사진 촬영 날짜(EXIF)

사진의 경우 가능하면 EXIF 촬영일을 우선한다.

---

## 4.5 파일 크기 기준

예:

```text
초소형   < 1MB
소형     1~10MB
중형     10~100MB
대형     100MB~1GB
초대형   > 1GB
```

추가 필터:

- 100MB 이상
- 500MB 이상
- 1GB 이상
- 사용자 지정

---

## 4.6 오래된 파일 기준

예:

```text
최근 7일
최근 30일
3개월 이상
6개월 이상
1년 이상
2년 이상
```

예:

```text
오래된 파일
└─ 1년 이상 사용하지 않은 파일
```

---

## 4.7 파일 이름 기준

파일명에 특정 키워드가 포함되면 지정 폴더로 이동할 수 있다.

예:

```text
계약
영수증
시험
성적
수업
출장
제주
여행
```

규칙 예:

```text
*영수증*
→ 문서/영수증
```

```text
*제주*
→ 여행/제주
```

---

# 5. 사용자 정의 규칙

사용자가 IF-AND-THEN 방식으로 규칙을 만들 수 있도록 한다.

예:

```text
IF
파일 확장자 = PDF
AND
파일명 contains "성적"

THEN
학교/성적 폴더 이동
```

지원 가능한 조건:

- 확장자
- 파일명
- 파일 크기
- 생성일
- 수정일
- 최근 사용일
- EXIF 날짜
- 파일 위치
- 상위 폴더명
- MIME 타입
- 해시값
- 특정 키워드
- 정규식

지원 가능한 동작:

- 이동
- 복사
- 이름 변경
- 폴더 생성
- 태그 부여
- 정리 제외
- 검토 대기
- 휴지통 이동

---

# 6. 규칙 우선순위

여러 조건이 동시에 일치할 수 있으므로 우선순위가 필요하다.

권장 순서:

```text
1. 보호 규칙
2. 사용자 지정 규칙
3. 개발 프로젝트 보호 규칙
4. 파일명 규칙
5. 날짜 규칙
6. 확장자 규칙
7. 기타
```

보호 규칙은 항상 가장 높은 우선순위를 가진다.

---

# 7. 중복 파일 탐색

중복 파일은 단순 파일명 비교가 아니라 실제 내용 비교를 지원해야 한다.

권장 단계:

1. 파일 크기 비교
2. 빠른 해시 비교
3. SHA-256 또는 동등 수준의 전체 해시 비교

예:

```text
사진.jpg
사진 (1).jpg
사진 - 복사본.jpg
```

표시 예:

```text
중복 파일 3개
총 215MB
```

삭제 전 반드시 사용자의 확인을 받는다.

---

# 8. 유사 파일 탐색

완전히 동일하지는 않지만 비슷한 이름이나 버전 관계를 가진 파일을 묶어 보여준다.

예:

```text
IMG_1234.jpg
IMG_1234_edit.jpg
IMG_1234_final.jpg
```

또는:

```text
보고서.docx
보고서_최종.docx
보고서_최종2.docx
보고서_진짜최종.docx
```

자동 삭제보다 **추천 및 검토 방식**으로 제공한다.

---

# 9. 버전 파일 정리

파일명 패턴을 분석하여 버전 관계를 추정한다.

예:

```text
보고서
보고서_수정
보고서_수정2
보고서_최종
보고서_최종2
보고서_진짜최종
```

추천 결과:

```text
최신 후보
이전 버전
```

자동 삭제 금지.
사용자가 직접 선택하도록 한다.

---

# 10. 빈 폴더 탐색

빈 폴더를 탐색하고 삭제 후보로 표시한다.

주의:

다음 파일이 존재하면 단순 빈 폴더로 간주하지 않는다.

```text
.gitkeep
desktop.ini
Thumbs.db
```

---

# 11. 깨진 바로가기 탐색

`.lnk` 파일의 대상 경로가 존재하지 않을 경우 깨진 바로가기로 표시한다.

사용자가 다음을 선택할 수 있게 한다.

- 삭제
- 무시
- 대상 다시 지정

---

# 12. 다운로드 폴더 전용 정리

다운로드 폴더는 별도의 빠른 정리 기능을 제공하는 것을 권장한다.

예:

```text
다운로드 정리
```

자동 분류:

```text
문서
이미지
설치파일
압축파일
동영상
기타
```

---

# 13. 바탕화면 정리

추천 구조:

```text
Desktop
├─ 문서
├─ 이미지
├─ 바로가기
├─ 작업중
└─ 임시파일
```

`.lnk` 바로가기는 기본적으로 이동하지 않는다.

---

# 14. 자동 파일명 정리

예:

```text
IMG_293847.jpg
```

→

```text
2026-09-12_제주_001.jpg
```

또는:

```text
다운로드 (4).pdf
```

→

```text
2026-09-12_다운로드.pdf
```

사용자 지정 규칙 예:

```text
날짜_원본이름_번호
```

---

# 15. 파일 이름 정제

자동 제거 또는 치환 가능한 패턴:

```text
복사본
copy
(1)
(2)
final
final2
다운로드
```

치환 예:

```text
공백 → _
_ → 공백
```

사용자가 미리보기 후 적용하도록 한다.

---

# 16. 연속 파일 이름 변경

Batch Rename 기능을 제공한다.

예:

```text
사진 001
사진 002
사진 003
```

지원 옵션:

- 시작 번호
- 자리수
- 접두사
- 접미사
- 날짜 추가
- 원본명 포함 여부

---

# 17. 정리 미리보기

정리 실행 전 반드시 결과를 미리 보여준다.

예:

```text
정리 예정

137개 파일 이동
34개 폴더 생성
12개 중복 발견

현재 위치 → 이동 위치
```

기본 원칙:

> **미리보기 없이 대량 정리를 실행하지 않는다.**

---

# 18. Dry Run

실제 이동 없이 결과만 시뮬레이션하는 기능을 제공한다.

예:

```text
🔍 정리 미리보기
```

Dry Run 결과에는 다음을 포함한다.

- 이동 예정 파일
- 이름 변경 예정 파일
- 생성 예정 폴더
- 충돌 파일
- 건너뛸 파일
- 정리 제외 파일
- 예상 확보 용량

---

# 19. Undo

필수 기능이다.

예:

```text
↩ 마지막 정리 되돌리기
```

기록 항목 예:

```text
A.pdf
Downloads → Documents/PDF
```

되돌리기 시 원래 경로 복원을 기본으로 한다.

---

# 20. 정리 기록

정리 실행 이력을 저장한다.

예:

```text
2026-09-12
다운로드 폴더 정리

파일 238개
이동 190
중복 24
정리 용량 2.3GB
```

권장 저장 항목:

- 실행 날짜
- 대상 폴더
- 적용 규칙
- 이동 파일 수
- 이름 변경 수
- 중복 파일 수
- 삭제/휴지통 파일 수
- 확보 공간
- Undo 가능 여부

---

# 21. 자동 정리 스케줄

지원 예:

```text
매일
매주
매월
PC 시작 시
다운로드 파일 100개 이상일 때
```

예:

```text
매주 일요일 오후 8시 다운로드 폴더 정리
```

자동 정리는 기본적으로 미리 정의된 안전 규칙 안에서만 실행한다.

---

# 22. 실시간 자동 정리

파일시스템 감시를 통해 새 파일을 실시간으로 분류할 수 있다.

예:

다운로드 폴더에 PDF가 들어오면:

```text
Documents/PDF
```

로 자동 이동.

주의:

- 다운로드 진행 중 파일 감지
- 잠긴 파일 처리
- 임시 파일 처리
- 브라우저 `.crdownload`, `.part` 파일 무시

---

# 23. 정리 모드

## 안전 모드

```text
이동만
삭제 없음
이름 변경 최소화
```

## 일반 모드

```text
이동
중복 추천
빈 폴더 정리
```

## 강력 정리

```text
오래된 설치파일
중복
임시파일
빈 폴더
대용량 파일
```

기본값은 **안전 모드**를 권장한다.

---

# 24. 예외 폴더

예:

```text
.git
node_modules
Windows
Program Files
OneDrive
Steam
게임
```

사용자가 직접 추가·삭제 가능해야 한다.

---

# 25. 예외 파일

예:

```text
*.env
*.key
*.pem
*.cert
```

민감하거나 중요한 파일은 이동 및 이름 변경에서 제외할 수 있어야 한다.

---

# 26. 파일/폴더 잠금

사용자가 특정 파일 또는 폴더를 직접 보호할 수 있어야 한다.

예:

```text
🔒 정리 제외
```

---

# 27. 대용량 파일 탐색

예:

```text
가장 큰 파일

Windows.iso        6.3GB
video.mp4          3.1GB
backup.zip         2.8GB
```

지원 기능:

- 크기순 정렬
- 폴더별 사용량
- 확장자별 사용량
- Treemap 시각화

---

# 28. 디스크 용량 분석

예:

```text
C 드라이브

문서       12GB
영상       83GB
이미지     27GB
프로그램   91GB
기타       42GB
```

정리 전후 용량 비교도 제공한다.

---

# 29. AI 기반 파일 분류

고급 기능으로 도입 가능하다.

예:

```text
2026제주일정최종.pdf
```

추천:

```text
여행 / 제주
```

예:

```text
2학년6반성적.xlsx
```

추천:

```text
학교 / 학생 / 성적
```

AI는 기본적으로 직접 이동하지 않고 **추천만 제공**한다.

---

# 30. 자연어 정리

사용자가 자연어로 정리 규칙을 입력할 수 있다.

예:

> 지난달 다운로드 받은 PDF는 문서 폴더로 옮기고 설치파일 중 30일 넘은 것은 따로 모아줘.

변환 결과 예:

```text
조건1
확장자 PDF
날짜 최근 1개월
→ Documents

조건2
EXE/MSI
30일 이상
→ Old Installers
```

실행 전 반드시 변환된 규칙을 사용자에게 보여준다.

---

# 31. 스마트 폴더 추천

파일명, 메타데이터, 폴더 구조를 분석해 추천할 수 있다.

예:

```text
추천 폴더

제주 여행
학교 업무
블로그 이미지
앱 개발
영수증
```

---

# 32. 사진 정리

EXIF 정보를 이용한다.

예:

```text
사진
└─ 2026
   ├─ 01
   ├─ 02
   └─ 09
```

또는 위치 기반:

```text
제주
서울
부산
대구
```

GPS 정보가 없는 경우 위치 분류를 하지 않는다.

---

# 33. 스크린샷 정리

권장 필터:

```text
7일 이상
30일 이상
90일 이상
```

예:

```text
90일 이상 된 스크린샷
823개
2.1GB
```

자동 삭제보다 검토 후 삭제를 권장한다.

---

# 34. 클라우드 파일 인식

지원 대상 예:

```text
OneDrive
Google Drive
Dropbox
```

주의사항:

- 온라인 전용 파일 강제 다운로드 금지
- 동기화 상태 확인
- 충돌 파일 처리
- 공유 폴더 보호
- 클라우드 경로 이동 시 사용자 경고

---

# 35. 권장 메인 화면

```text
┌────────────────────────────┐
│ 🗂 FileFlow                │
├────────────────────────────┤
│                            │
│   다운로드  1,283 files    │
│   바탕화면    143 files    │
│   문서        834 files    │
│                            │
│   [ 🔍 파일 분석 ]         │
│                            │
├────────────────────────────┤
│ 빠른 정리                  │
│                            │
│ 📄 문서        234         │
│ 🖼 이미지       537         │
│ 🎬 영상         32         │
│ 📦 압축         42         │
│ ⚙ 설치파일      18         │
│ 🧑‍💻 개발파일    97         │
├────────────────────────────┤
│ 중복파일 124개             │
│ 확보 가능 공간 7.8 GB      │
│                            │
│ [정리 미리보기]            │
└────────────────────────────┘
```

---

# 36. 권장 기본 카테고리

```text
📄 문서
   ├ PDF
   ├ Word
   ├ Excel
   ├ PowerPoint
   ├ 한글
   └ Text

🖼 이미지
   ├ 사진
   ├ 스크린샷
   └ 디자인

🎬 동영상

🎵 오디오

📦 압축파일

⚙ 설치파일

💻 개발파일

📚 전자책

🔤 폰트

💾 디스크 이미지

🗃 데이터

🧾 영수증

🕐 오래된 파일

❓ 기타
```

---

# 37. 사용자 초기 설정 질문

## 1단계. 어디를 정리할까요?

```text
□ 다운로드
□ 바탕화면
□ 문서
□ 사진
□ 사용자 지정
```

## 2단계. 어떤 기준으로 정리할까요?

```text
● 파일 종류
○ 날짜
○ 파일 크기
○ 파일 이름
○ AI 추천
```

## 3단계. 하위 폴더도 정리할까요?

```text
YES / NO
```

## 4단계. 중복 파일을 찾을까요?

```text
YES / NO
```

## 5단계. 오래된 파일도 찾을까요?

```text
30일
90일
180일
1년
```

## 6단계. 자동 정리를 사용할까요?

```text
OFF
매일
매주
매월
```

---

# 38. 필수 안전장치

반드시 포함할 기능:

1. 정리 미리보기
2. Dry Run
3. 삭제보다 휴지통 사용
4. Undo
5. 정리 기록
6. 시스템 폴더 보호
7. 개발 프로젝트 보호
8. 사용 중인 파일 보호
9. 중복 삭제 전 확인
10. 대량 이동 전 경고
11. 파일 충돌 처리
12. 예외 폴더
13. 예외 파일
14. 잠금 파일/폴더
15. 정리 실패 로그

파일명 충돌 시 선택지:

```text
덮어쓰기
건너뛰기
이름 변경
```

기본값은:

```text
이름 변경
```

을 권장한다.

---

# 39. 파일 충돌 처리 규칙

동일 경로에 동일 파일명이 존재할 경우:

## 기본 정책

```text
파일.pdf
파일 (1).pdf
파일 (2).pdf
```

처럼 자동 번호를 붙인다.

## 선택 가능한 정책

- 자동 이름 변경
- 건너뛰기
- 최신 파일 유지
- 큰 파일 유지
- 직접 선택
- 덮어쓰기

덮어쓰기는 기본 비활성화 권장.

---

# 40. 오류 처리

다음 상황을 별도로 처리한다.

- 파일 사용 중
- 권한 없음
- 관리자 권한 필요
- 경로 너무 김
- 지원하지 않는 문자
- 동일 파일명 충돌
- 네트워크 연결 끊김
- 외장 디스크 분리
- 클라우드 동기화 중
- 파일 삭제됨
- 파일 경로 변경됨
- 읽기 전용 파일

오류 발생 파일은 별도 목록에 남긴다.

---

# 41. 앱 MVP 권장 범위

## v1

1. 폴더 선택
2. 확장자별 자동 분류
3. 사용자 정의 카테고리
4. 정리 미리보기
5. 파일 이동
6. Undo
7. 중복 파일 탐색
8. 정리 기록

---

## v2

추가 기능:

- 날짜별 정리
- 대용량 파일 탐색
- 오래된 파일 탐색
- 자동 파일명 변경
- 정리 스케줄
- 바탕화면 빠른 정리
- 다운로드 빠른 정리

---

## v3

고급 기능:

- 자연어 정리
- AI 파일 분류
- 스마트 폴더
- 유사 파일 탐지
- 사진 EXIF 분류
- 실시간 자동 정리

---

# 42. 차별화 전략

단순 확장자 분류만으로는 경쟁력이 약하다.

앱의 차별점은 다음 방향을 권장한다.

## 핵심 가치

> **안전하게 자동 정리하고, 언제든 되돌릴 수 있다.**

## 대표 흐름

```text
정리할 폴더 선택
↓
파일 분석
↓
정리 계획 생성
↓
사용자 미리보기
↓
승인
↓
정리 실행
↓
기록 저장
↓
필요 시 Undo
```

---

# 43. 개발 프로젝트 보호 차별화

다음 구조를 자동 감지하면 매우 유용하다.

```text
.git
package.json
node_modules
src
public
.env
requirements.txt
pyproject.toml
Cargo.toml
```

해당 폴더는:

```text
🛡 개발 프로젝트 감지
자동 정리 제외
```

로 처리한다.

---

# 44. 추천 제품 원칙

앱 전체 설계 시 다음 원칙을 유지한다.

## 원칙 1

삭제보다 정리를 우선한다.

## 원칙 2

자동 실행보다 미리보기를 우선한다.

## 원칙 3

AI 판단보다 사용자 승인을 우선한다.

## 원칙 4

분류 정확도보다 복구 가능성을 우선한다.

## 원칙 5

시스템 및 개발 프로젝트를 보호한다.

## 원칙 6

모든 대량 변경은 기록한다.

## 원칙 7

자동화는 신뢰가 쌓인 뒤 단계적으로 허용한다.

---

# 45. 최종 제품 정의

이 앱은 단순한 디스크 클리너가 아니라 다음과 같은 제품으로 정의한다.

> **Windows의 다운로드, 바탕화면, 문서, 이미지, 개발파일 등을 사용자가 정한 규칙에 따라 안전하게 분류하고, 중복·대용량·오래된 파일을 찾아주며, 정리 전 미리보기와 정리 후 Undo를 제공하는 스마트 파일 관리 앱**

핵심 사용자 경험은 다음과 같다.

```text
분석
→ 추천
→ 미리보기
→ 승인
→ 정리
→ 기록
→ 복구
```

---

# 46. 개발 시 최우선 체크리스트

## 필수

- [ ] 정리 대상 폴더 선택
- [ ] 파일 형식 분류
- [ ] 문서/HWP/HWPX 지원
- [ ] 이미지 분류
- [ ] 동영상 분류
- [ ] 오디오 분류
- [ ] 압축파일 분류
- [ ] 설치파일 분류
- [ ] 개발파일 분류
- [ ] 사용자 지정 규칙
- [ ] 시스템 폴더 보호
- [ ] 개발 프로젝트 보호
- [ ] Dry Run
- [ ] 미리보기
- [ ] Undo
- [ ] 로그
- [ ] 중복 탐색
- [ ] 파일 충돌 처리
- [ ] 오류 처리

## 권장

- [ ] 날짜별 정리
- [ ] EXIF 기반 사진 정리
- [ ] 대용량 파일 분석
- [ ] 오래된 파일 탐색
- [ ] 빈 폴더 탐색
- [ ] 깨진 바로가기 탐색
- [ ] 자동 이름 변경
- [ ] Batch Rename
- [ ] 다운로드 빠른 정리
- [ ] 바탕화면 빠른 정리
- [ ] 클라우드 폴더 감지

## 고급

- [ ] AI 분류
- [ ] 자연어 정리
- [ ] 스마트 폴더 추천
- [ ] 유사 파일 탐지
- [ ] 실시간 정리
- [ ] 자동 스케줄
- [ ] Treemap 시각화

---

# 47. 한 줄 핵심 원칙

> **정리 앱의 품질은 얼마나 많이 옮기느냐가 아니라, 얼마나 안전하게 분류하고 완벽하게 되돌릴 수 있느냐로 평가한다.**
